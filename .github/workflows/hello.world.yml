name: hello world demo
on:
	push:
		branches:
		  	main

jobs:
	say hello:
		run-on: ubuntu-latest
		steps:
				name: Checkout code
				uses: actions/checkout@v3

				name: print Hello
				run: echo "Hello, Github Actions! "

